# StudentFlow Environment Variables (example)

# Secret key for JWT tokens
SECRET_KEY=replace-in-cloud

# Google Gemini API Key for AI features
GEMINI_API_KEY=replace-in-cloud

# Optional legacy API key
GOOGLE_API_KEY=replace-in-cloud

# Database path for local/dev
DB_PATH=./data/studentflow.db

# Firebase Configuration (public at runtime but do not commit real values)
FIREBASE_API_KEY=replace-in-cloud
FIREBASE_AUTH_DOMAIN=studentflow-<project>.firebaseapp.com
FIREBASE_PROJECT_ID=studentflow-<project>
FIREBASE_STORAGE_BUCKET=studentflow-<project>.firebasestorage.app
FIREBASE_MESSAGING_SENDER_ID=replace-in-cloud
FIREBASE_APP_ID=replace-in-cloud
FIREBASE_MEASUREMENT_ID=
FIREBASE_DATABASE_URL=https://studentflow-<project>-default-rtdb.firebaseio.com

# Optional: JSON string for service account (prefer Secret Manager)
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account",...}
